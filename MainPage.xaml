<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="WayofLife.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Home"
    MinimumWidthRequest="300">

    <!--<ContentPage.ToolbarItems>
        <ToolbarItem Clicked="ToolbarItem_Clicked" Text="Refresh" />
    </ContentPage.ToolbarItems>-->

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            MaximumWidthRequest="500"
            Spacing="25">

            <Label
                FontSize="Title"
                HorizontalOptions="Center"
                Text="Motivation Mode"
                VerticalOptions="Center" />
            <Picker x:Name="picker_motivationModes" SelectedIndexChanged="Picker_SelectedIndexChanged" />

            <Label
                x:Name="QuoteLabel"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                VerticalOptions="Center" />

            <!--<Entry
                x:DataType="Entry"
                MaximumWidthRequest="400"
                Placeholder="How are you today?"
                Text="{Binding Text}" />-->

            <!--<CollectionView
                Grid.Row="2"
                Grid.ColumnSpan="2"
                ItemsSource="{Binding Items}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="{x:Type x:String}">
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem BackgroundColor="Red" Text="Delete" />
                                </SwipeItems>
                            </SwipeView.RightItems>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>-->

        </VerticalStackLayout>
    </ScrollView>


</ContentPage>
